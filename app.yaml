# Databricks App Configuration
command: ["streamlit", "run", "rag_ui_app.py", "--server.port=8080"]

env:
  - name: GROQ_API_KEY1
    value: "${secrets/groq/api_key}"  # Reference to Databricks secret
